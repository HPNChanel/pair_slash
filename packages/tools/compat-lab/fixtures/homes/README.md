# Simulated Homes

These directories reserve user-scope config-home fixtures for compat-lab smoke tests.

- `codex-user/`
- `copilot-user/`

Phase 4 uses temp homes during execution for deterministic isolation. The checked-in layout exists so the lab structure is explicit and can grow without changing package boundaries.
