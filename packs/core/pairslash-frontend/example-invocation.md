# Example Invocation

```
/skills
```

Select `pairslash-frontend`, then provide a bounded frontend task such as:

> Add an empty state and retry flow to the billing history page without changing
> the existing API contract.
