# Example Invocation

```
/skills
```

Select `pairslash-devops`, then provide a bounded operational task such as:

> Add a manual approval gate to the production deploy workflow without changing
> staging behavior.
