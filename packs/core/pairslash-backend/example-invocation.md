# Example Invocation

```
/skills
```

Select `pairslash-backend`, then provide a bounded backend task such as:

> Add idempotency protection to the webhook ingestion endpoint without changing
> the public response contract.
