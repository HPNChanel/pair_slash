# pairslash-command-suggest -- Example Invocation

```text
/skills
```

Select `pairslash-command-suggest`, then provide:

```yaml
intent: "Run all Phase 2 validation checks"
platform: windows
scope_hint: "repo root"
```

